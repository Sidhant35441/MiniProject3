spring:
  datasource:
      driver-class-name: org.h2.Driver
      password: 
      url: jdbc:h2:mem:testdb
      username: sa   
  jpa:
    properties:
      hibernate:
      format-sql: true
      show_sql: true
  
server:
  port: 2020
  servlet:
    context-path: /SSA